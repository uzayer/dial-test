// Landing-page type scale, shared so every section reads as one page.
export const eyebrow =
  "text-sm font-medium uppercase tracking-[0.22em] text-muted-foreground";
export const sectionHeading =
  "text-balance text-4xl font-semibold tracking-tight md:text-5xl";

// Editorial scale for directory and detail pages (see PageHeader). Titles are
// set in the display serif; everything else stays in Geist.
export const pageTitle =
  "font-display text-5xl font-normal leading-[0.95] tracking-tight text-balance md:text-7xl";
export const displaySectionTitle =
  "font-display text-3xl font-normal leading-tight tracking-tight md:text-4xl";
export const lede = "max-w-prose text-pretty text-lg text-muted-foreground";
/** Small uppercase label that sits above a title or a column of metadata. */
export const label =
  "text-xs font-medium uppercase tracking-[0.18em] text-muted-foreground";
